import TodoList from './components/TodoList.tsx';

const App = () => {
    return (
        <main className="container font-nunito text-2xl py-10">
            <TodoList />
        </main>
    );
};

export default App;
